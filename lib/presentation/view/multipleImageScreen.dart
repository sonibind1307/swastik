import 'package:flutter/material.dart';

class MultiImageScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("MultiPleImage"),
      ),
      body: Container(),
    );
  }
}
